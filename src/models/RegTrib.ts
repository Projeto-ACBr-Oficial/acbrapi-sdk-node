/* tslint:disable */
/* eslint-disable */
/**
 * ACBr API
 * API para automação comercial e documentos fiscais.    **Autenticação**  Esta API usa OAuth 2.0 no fluxo `client_credentials`.    **Ambientes disponíveis**  - Produção: `https://prod.acbr.api.br`  - Homologação: `https://hom.acbr.api.br`  - Autenticação para ambos os ambientes: `https://auth.acbr.api.br/realms/ACBrAPI/protocol/openid-connect/token`    **Endpoint para obter o token**  - `POST https://auth.acbr.api.br/realms/ACBrAPI/protocol/openid-connect/token`    **Content-Type**  - `application/x-www-form-urlencoded`    **Parâmetros do corpo**  - `grant_type=client_credentials`  - `client_id=SEU_CLIENT_ID`  - `client_secret=SEU_CLIENT_SECRET`  - `scope=empresa nfe nfse`    **Exemplo de corpo**  - `grant_type=client_credentials&client_id=SEU_CLIENT_ID&client_secret=SEU_CLIENT_SECRET&scope=empresa%20nfe`    **Uso do token nas requisições**  - `Authorization: Bearer SEU_ACCESS_TOKEN`
 *
 * The version of the OpenAPI document: 3.1.7
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from '../runtime';
/**
 * Grupo de informações relativas aos regimes de tributação do prestador de serviços.
 * @export
 * @interface RegTrib
 */
export interface RegTrib {
    /**
     * Regime Especial de Tributação da Prefeitura.
     * 
     * Campo opcional utilizado no momento da emissão da NFS-e para informar
     * diretamente o código de regime especial de tributação esperado pela
     * prefeitura.
     * 
     * **Comportamento:**
     * - Ao preencher este campo, o valor será inserido diretamente no XML
     *   da NFS-e, sem qualquer conversão pela API.
     * - Esse campo sobrescreve o valor configurado previamente nas
     *   configurações de NFS-e da empresa.
     * - - especialmente útil quando a prefeitura utiliza códigos próprios que
     *   não existem no padrão nacional (como valores diferentes dos listados
     *   de 0 a 6).
     * 
     * **Cenários de uso:**
     * - Quando a prefeitura exige um código que não pode ser representado
     *   pelos valores do padrão nacional.
     * - Quando houver necessidade de enviar o código exato esperado pela
     *   prefeitura, sem depender da lógica de conversão automática da API.
     * 
     * **Atenção**: Para emissões pelo Sistema Nacional NFS-e, esse campo é ignorado.
     * @type {number}
     * @memberof RegTrib
     */
    regEspTrib?: number | null;
}

/**
 * Check if a given object implements the RegTrib interface.
 */
export function instanceOfRegTrib(value: object): boolean {
    let isInstance = true;

    return isInstance;
}

export function RegTribFromJSON(json: any): RegTrib {
    return RegTribFromJSONTyped(json, false);
}

export function RegTribFromJSONTyped(json: any, ignoreDiscriminator: boolean): RegTrib {
    if ((json === undefined) || (json === null)) {
        return json;
    }
    return {
        
        'regEspTrib': !exists(json, 'regEspTrib') ? undefined : json['regEspTrib'],
    };
}

export function RegTribToJSON(value?: RegTrib | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        
        'regEspTrib': value.regEspTrib,
    };
}

