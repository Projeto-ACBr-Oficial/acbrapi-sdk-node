/* tslint:disable */
/* eslint-disable */
/**
 * ACBr API
 * API para automação comercial e documentos fiscais.    **Autenticação**  Esta API usa OAuth 2.0 no fluxo `client_credentials`.    **Ambientes disponíveis**  - Produção: `https://prod.acbr.api.br`  - Homologação: `https://hom.acbr.api.br`  - Autenticação para ambos os ambientes: `https://auth.acbr.api.br/realms/ACBrAPI/protocol/openid-connect/token`    **Endpoint para obter o token**  - `POST https://auth.acbr.api.br/realms/ACBrAPI/protocol/openid-connect/token`    **Content-Type**  - `application/x-www-form-urlencoded`    **Parâmetros do corpo**  - `grant_type=client_credentials`  - `client_id=SEU_CLIENT_ID`  - `client_secret=SEU_CLIENT_SECRET`  - `scope=empresa nfe nfse`    **Exemplo de corpo**  - `grant_type=client_credentials&client_id=SEU_CLIENT_ID&client_secret=SEU_CLIENT_SECRET&scope=empresa%20nfe`    **Uso do token nas requisições**  - `Authorization: Bearer SEU_ACCESS_TOKEN`
 *
 * The version of the OpenAPI document: 3.1.7
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists, mapValues } from '../runtime';
import type { DfeAutorizacao } from './DfeAutorizacao';
import {
    DfeAutorizacaoFromJSON,
    DfeAutorizacaoFromJSONTyped,
    DfeAutorizacaoToJSON,
} from './DfeAutorizacao';

/**
 * 
 * @export
 * @interface Dfe
 */
export interface Dfe {
    /**
     * ID único gerado pela API para este documento.
     * @type {string}
     * @memberof Dfe
     */
    id?: string;
    /**
     * 
     * @type {string}
     * @memberof Dfe
     */
    ambiente?: DfeAmbienteEnum;
    /**
     * Data/hora em que o documento foi criado na API. Representado no formato <a href="https://en.wikipedia.org/wiki/ISO_8601" target="blank">`ISO 8601`</a>.
     * @type {Date}
     * @memberof Dfe
     */
    created_at?: Date;
    /**
     * * `pendente`: o pedido de emissão do documento foi recebido pela API e está na fila de processamento.
     * * `autorizado`, `rejeitado` ou `denegado`: o documento foi transmitido para a SEFAZ, que retornou um desses status.
     * * `cancelado`: um evento de cancelamento foi homologado pela SEFAZ e associado ao documento.
     * * `encerrado`: um evento de encerramento foi homologado pela SEFAZ e associado a um MDF-e.
     * * `erro`: status próprio da API que significa, na maioria das vezes, que houve algum erro que impediu a transmissão do documento para a SEFAZ (erros de validação, erros interno do servidor, timeouts, etc).
     * @type {string}
     * @memberof Dfe
     */
    status?: DfeStatusEnum;
    /**
     * Seu identificador único para este documento. Opcional, ajuda a evitar o envio duplicado de um mesmo documento.
     * @type {string}
     * @memberof Dfe
     */
    referencia?: string | null;
    /**
     * 
     * @type {Date}
     * @memberof Dfe
     */
    data_emissao?: Date;
    /**
     * 
     * @type {number}
     * @memberof Dfe
     */
    modelo?: number;
    /**
     * 
     * @type {number}
     * @memberof Dfe
     */
    serie?: number;
    /**
     * 
     * @type {number}
     * @memberof Dfe
     */
    numero?: number;
    /**
     * 
     * @type {number}
     * @memberof Dfe
     */
    tipo_emissao?: number;
    /**
     * 
     * @type {number}
     * @memberof Dfe
     */
    valor_total?: number;
    /**
     * Chave de acesso do DF-e.
     * @type {string}
     * @memberof Dfe
     */
    chave?: string;
    /**
     * 
     * @type {DfeAutorizacao}
     * @memberof Dfe
     */
    autorizacao?: DfeAutorizacao;
}


/**
 * @export
 */
export const DfeAmbienteEnum = {
    Homologacao: 'homologacao',
    Producao: 'producao'
} as const;
export type DfeAmbienteEnum = typeof DfeAmbienteEnum[keyof typeof DfeAmbienteEnum];

/**
 * @export
 */
export const DfeStatusEnum = {
    Pendente: 'pendente',
    Autorizado: 'autorizado',
    Rejeitado: 'rejeitado',
    Denegado: 'denegado',
    Encerrado: 'encerrado',
    Cancelado: 'cancelado',
    Erro: 'erro'
} as const;
export type DfeStatusEnum = typeof DfeStatusEnum[keyof typeof DfeStatusEnum];


/**
 * Check if a given object implements the Dfe interface.
 */
export function instanceOfDfe(value: object): boolean {
    let isInstance = true;

    return isInstance;
}

export function DfeFromJSON(json: any): Dfe {
    return DfeFromJSONTyped(json, false);
}

export function DfeFromJSONTyped(json: any, ignoreDiscriminator: boolean): Dfe {
    if ((json === undefined) || (json === null)) {
        return json;
    }
    return {
        
        'id': !exists(json, 'id') ? undefined : json['id'],
        'ambiente': !exists(json, 'ambiente') ? undefined : json['ambiente'],
        'created_at': !exists(json, 'created_at') ? undefined : (new Date(json['created_at'])),
        'status': !exists(json, 'status') ? undefined : json['status'],
        'referencia': !exists(json, 'referencia') ? undefined : json['referencia'],
        'data_emissao': !exists(json, 'data_emissao') ? undefined : (new Date(json['data_emissao'])),
        'modelo': !exists(json, 'modelo') ? undefined : json['modelo'],
        'serie': !exists(json, 'serie') ? undefined : json['serie'],
        'numero': !exists(json, 'numero') ? undefined : json['numero'],
        'tipo_emissao': !exists(json, 'tipo_emissao') ? undefined : json['tipo_emissao'],
        'valor_total': !exists(json, 'valor_total') ? undefined : json['valor_total'],
        'chave': !exists(json, 'chave') ? undefined : json['chave'],
        'autorizacao': !exists(json, 'autorizacao') ? undefined : DfeAutorizacaoFromJSON(json['autorizacao']),
    };
}

export function DfeToJSON(value?: Dfe | null): any {
    if (value === undefined) {
        return undefined;
    }
    if (value === null) {
        return null;
    }
    return {
        
        'id': value.id,
        'ambiente': value.ambiente,
        'created_at': value.created_at === undefined ? undefined : (value.created_at.toISOString()),
        'status': value.status,
        'referencia': value.referencia,
        'data_emissao': value.data_emissao === undefined ? undefined : (value.data_emissao.toISOString()),
        'modelo': value.modelo,
        'serie': value.serie,
        'numero': value.numero,
        'tipo_emissao': value.tipo_emissao,
        'valor_total': value.valor_total,
        'chave': value.chave,
        'autorizacao': DfeAutorizacaoToJSON(value.autorizacao),
    };
}

